#include<stdio.h>
main()
{
    int n,sum=0,mod,num;
    printf("Enter a number : ");
    scanf("%d",&n);
    num=n;
    while(n!=0){
    	mod=n%10;
    	sum=sum+mod;
    	n=n/10;
	}
	printf("Sum of digits of %d = %d",num,sum);
}
